# Azure DevOps Pipeline definition
pool: 
  name: Default
stages:
- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - task: NuGetToolInstaller@1
      inputs:
        versionSpec: 
    - task: DotNetCoreCLI@2
      inputs:
        command: 'build'
        projects: '**/StringLibrary/*.csproj'
- stage: Deploy
  jobs:
  - job: PackPush
    steps:
    - task: DotNetCoreCLI@2
      inputs:
        command: 'pack'
        packagesToPack: '**/StringLibrary/*.csproj'
        buildProperties: 'PackageId=Patrick_COMP367_Lab4_StringLibrary'
        versioningScheme: 'off'
    - task: NuGetCommand@2
      inputs:
        command: 'push'
        packagesToPush: '$(Build.ArtifactStagingDirectory)/**/*.nupkg;!$(Build.ArtifactStagingDirectory)/**/*.symbols.nupkg'
        nuGetFeedType: 'internal'
        publishVstsFeed: '38c78008-2919-4d51-8286-54415c7d7b78'
        allowPackageConflicts: true